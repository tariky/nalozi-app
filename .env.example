# Copy to .env and fill in real values (or set these in the Coolify UI).

# Required: OpenRouter API key used by the invoice-scan OCR endpoint.
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional overrides (defaults shown — already set in the Dockerfile/compose).
# PORT=3000
# DB_PATH=/app/data/asnord.db
# NODE_ENV=production
